-- DRAGON PINGX ULTIMATE - QUANGANH EDITION
-- ADMIN KEY: QANHNO1CRACKER
-- WEBSITE: https://roszmodxqanhno1.onrender.com

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera
local SoundService = game:GetService("SoundService")
local player = Players.LocalPlayer
local mouse = player:GetMouse()

local API_URL = "https://roszmodxqanhno1.onrender.com"
local isVerified = false
local isLoggedIn = false
local menuVisible = true
local menuPosition = UDim2.new(0.5, -300, 0.5, -350)

local ICON_URL = "https://cdn.discordapp.com/attachments/1507622651554697339/1507623741654569011/IMG_20260523_125754_302.jpg"

-- ========== SPECIAL FREEZE VARIABLES ==========
local freezeActive = false
local freezeConnection = nil
local soundConnection = nil
local textParts = {}
local bloodSkyConnection = nil

-- ========== ESP VARIABLES ==========
local espEnabled = false
local espBox2D = false
local espCornerBox = false
local espTracer = false
local espName = false
local espDistance = false
local espHealth = false
local espHealthBar = false
local espHeadDot = false
local espChams = false
local espChamsOutline = false
local espHighlight = false
local espRadar = false
local espOffscreen = false
local espTeamCheck = true
local espRainbow = false

-- ========== AIMBOT VARIABLES ==========
local aimbotEnabled = false
local aimbotStandard = false
local aimbotLock = false
local aimbotSmooth = false
local aimbotFOV = false
local silentAimEnabled = false
local aimbotFOVSize = 150
local aimbotSmoothValue = 5
local aimbotFOVCircle = nil

-- ========== MOVEMENT HACK VARIABLES ==========
local speedEnabled = false
local speedValue = 50
local jumpEnabled = false
local jumpValue = 150
local infinityJumpEnabled = false
local flyEnabled = false
local noclipEnabled = false
local flyBodyVelocity = nil
local noclipConnections = {}
local infinityJumpConn = nil

-- ========== AUTO & FARM VARIABLES ==========
local autoClickEnabled = false
local autoFarmEnabled = false
local autoClickConnection = nil

local espObjects = {}
local drawingObjects = {}
local highlightObjects = {}

local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")

-- ========== UTILITIES ==========
local function notify(msg)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "DRAGON PINGX",
            Text = msg,
            Duration = 2
        })
    end)
end

local function toast(msg, duration)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 40)
    frame.Position = UDim2.new(0.5, -125, 0.92, 0)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
    frame.BackgroundTransparency = 0.2
    frame.BorderSizePixel = 1
    frame.BorderColor3 = Color3.fromRGB(255, 0, 150)
    frame.Parent = screenGui
    
    local text = Instance.new("TextLabel")
    text.Size = UDim2.new(1, 0, 1, 0)
    text.BackgroundTransparency = 1
    text.Text = msg
    text.TextColor3 = Color3.fromRGB(255, 255, 255)
    text.TextSize = 13
    text.Font = Enum.Font.GothamSemibold
    text.Parent = frame
    
    local tween1 = TweenService:Create(frame, TweenInfo.new(0.3), {Position = UDim2.new(0.5, -125, 0.88, 0)})
    tween1:Play()
    task.wait(duration or 2)
    local tween2 = TweenService:Create(frame, TweenInfo.new(0.3), {Position = UDim2.new(0.5, -125, 0.95, 0), BackgroundTransparency = 1})
    tween2:Play()
    tween2.Completed:Connect(function() frame:Destroy() end)
end

local function getHWID()
    local result = "DRAGON-" .. player.UserId
    if syn and syn.crypt then
        result = syn.crypt.custom_hash(player.UserId)
    elseif getexecutorname then
        result = getexecutorname() .. "-" .. player.UserId
    end
    return result
end

local function verifyKey(key)
    if key == "QANHNO1CRACKER" or key == "DRAGONLOCUT" then
        return true
    end
    local url = API_URL .. "/api/verify?key=" .. key .. "&hwid=" .. getHWID()
    local success, res = pcall(function()
        return game:HttpGet(url)
    end)
    if success and res then
        local data = HttpService:JSONDecode(res)
        return data.status == "success"
    end
    return false
end

-- ========== SPECIAL FREEZE FUNCTIONS (LAG ALL OTHER PLAYERS) ==========
local function clearSkyText()
    for _, part in pairs(textParts) do
        pcall(function() part:Destroy() end)
    end
    textParts = {}
end

local function createSkyText()
    clearSkyText()
    local textContent = "QUANGANH IS THE KING"
    local colors = {Color3.fromRGB(255, 0, 0), Color3.fromRGB(200, 0, 0), Color3.fromRGB(255, 50, 50)}
    
    for i = 1, 30 do
        local billboard = Instance.new("BillboardGui")
        billboard.Name = "KingText_" .. i
        billboard.Size = UDim2.new(0, math.random(300, 600), 0, math.random(100, 200))
        billboard.StudsOffset = Vector3.new(math.random(-500, 500), math.random(50, 300), math.random(-500, 500))
        billboard.AlwaysOnTop = true
        billboard.Parent = workspace
        
        local textLabel = Instance.new("TextLabel")
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.Text = textContent
        textLabel.TextColor3 = colors[math.random(1, 3)]
        textLabel.TextScaled = true
        textLabel.TextStrokeTransparency = 0.3
        textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        textLabel.Font = Enum.Font.GothamBlack
        textLabel.TextWrapped = true
        textLabel.Parent = billboard
        
        table.insert(textParts, billboard)
        
        -- Animation for floating text
        spawn(function()
            local startY = billboard.StudsOffset.Y
            local direction = 1
            while billboard and billboard.Parent do
                local newY = startY + math.sin(tick() * 2 + i) * 20
                billboard.StudsOffset = Vector3.new(billboard.StudsOffset.X, newY, billboard.StudsOffset.Z)
                task.wait(0.05)
            end
        end)
    end
end

local function createBloodSky()
    if bloodSkyConnection then
        bloodSkyConnection:Disconnect()
    end
    
    -- Set sky to blood red
    local originalAmbient = Lighting.Ambient
    local originalOutdoor = Lighting.OutdoorAmbient
    local originalFogColor = Lighting.FogColor
    local originalClockTime = Lighting.ClockTime
    
    Lighting.Ambient = Color3.fromRGB(100, 0, 0)
    Lighting.OutdoorAmbient = Color3.fromRGB(80, 0, 0)
    Lighting.FogColor = Color3.fromRGB(150, 0, 0)
    Lighting.FogEnd = 1000
    Lighting.ClockTime = 0
    
    bloodSkyConnection = RunService.RenderStepped:Connect(function()
        if not freezeActive then
            return
        end
        local r = 80 + math.sin(tick() * 5) * 30
        Lighting.Ambient = Color3.fromRGB(r, 0, 0)
        Lighting.OutdoorAmbient = Color3.fromRGB(r - 20, 0, 0)
    end)
end

local function restoreSky()
    if bloodSkyConnection then
        bloodSkyConnection:Disconnect()
        bloodSkyConnection = nil
    end
    Lighting.Ambient = Color3.fromRGB(0, 0, 0)
    Lighting.OutdoorAmbient = Color3.fromRGB(0, 0, 0)
    Lighting.FogColor = Color3.fromRGB(0, 0, 0)
    Lighting.ClockTime = 14
    clearSkyText()
end

local function playLaughSound()
    -- Create a creepy laughing sound that everyone hears
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://9120386436"
    sound.Volume = 5
    sound.PlayOnRemove = true
    sound.Parent = workspace
    pcall(function() sound:Play() end)
    
    -- Schedule next laugh
    if freezeActive then
        task.wait(math.random(5, 15))
        if freezeActive then
            playLaughSound()
        end
    end
end

local function startFreeze()
    if freezeActive then
        toast("Freeze already running!")
        return
    end
    freezeActive = true
    toast("🔥 FREEZE SPECIAL ACTIVATED! All other players frozen!")
    
    -- Create blood sky
    createBloodSky()
    
    -- Create floating text
    createSkyText()
    
    -- Start creepy laugh
    playLaughSound()
    
    -- Freeze all other players (they appear frozen but you move normally)
    freezeConnection = RunService.RenderStepped:Connect(function()
        if not freezeActive then
            return
        end
        
        -- Freeze all other players' characters (they can't move)
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character then
                local humanoidChar = plr.Character:FindFirstChild("Humanoid")
                if humanoidChar then
                    humanoidChar.WalkSpeed = 0
                    humanoidChar.JumpPower = 0
                end
                local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                end
            end
        end
        
        -- Spam remote events to cause lag for others
        local remotes = {}
        local function findRemotes(p)
            for _, v in pairs(p:GetChildren()) do
                if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
                    table.insert(remotes, v)
                end
                findRemotes(v)
            end
        end
        findRemotes(ReplicatedStorage)
        
        for _, r in pairs(remotes) do
            pcall(function()
                if r:IsA("RemoteEvent") then
                    r:FireServer(string.rep("FREEZE_LAG", 200))
                else
                    r:InvokeServer(string.rep("FREEZE_LAG", 200))
                end
            end)
        end
    end)
    
    toast("❄️ Other players are now FROZEN! Sky turned BLOOD RED!", 5)
    notify("QUANGANH IS THE KING - All hail the king!")
end

local function stopFreeze()
    if not freezeActive then
        toast("Freeze is not running!")
        return
    end
    freezeActive = false
    if freezeConnection then
        freezeConnection:Disconnect()
        freezeConnection = nil
    end
    
    -- Restore all players' movement
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local humanoidChar = plr.Character:FindFirstChild("Humanoid")
            if humanoidChar then
                humanoidChar.WalkSpeed = 16
                humanoidChar.JumpPower = 50
            end
        end
    end
    
    -- Restore sky
    restoreSky()
    
    toast("FREEZE STOPPED - Everything back to normal")
end

local function toggleFreeze()
    if freezeActive then
        stopFreeze()
    else
        startFreeze()
    end
end

-- ========== ESP FUNCTIONS ==========
local function getESPColor(plr)
    if espRainbow then
        local r = (math.sin(tick() * 2) + 1) / 2
        local g = (math.sin(tick() * 2 + 2) + 1) / 2
        local b = (math.sin(tick() * 2 + 4) + 1) / 2
        return Color3.new(r, g, b)
    end
    if espTeamCheck and plr.Team == player.Team then
        return Color3.fromRGB(0, 255, 0)
    end
    return Color3.fromRGB(255, 0, 0)
end

local function clearESP()
    for _, obj in pairs(drawingObjects) do
        pcall(function() obj:Remove() end)
    end
    drawingObjects = {}
    for _, obj in pairs(highlightObjects) do
        pcall(function() obj:Destroy() end)
    end
    highlightObjects = {}
    for _, obj in pairs(espObjects) do
        pcall(function() obj:Destroy() end)
    end
    espObjects = {}
end

local function updateESP()
    if not espEnabled then
        return
    end
    clearESP()
    
    for _, plr in pairs(Players:GetPlayers()) do
        local shouldSkip = false
        
        if plr == player then
            shouldSkip = true
        end
        if espTeamCheck and plr.Team == player.Team then
            shouldSkip = true
        end
        if not plr.Character or not plr.Character:FindFirstChild("HumanoidRootPart") then
            shouldSkip = true
        end
        
        if not shouldSkip then
            local hrp = plr.Character.HumanoidRootPart
            local pos, onScreen = Camera:WorldToScreenPoint(hrp.Position)
            local humanoidChar = plr.Character:FindFirstChild("Humanoid")
            local healthPercent = 1
            if humanoidChar then
                healthPercent = humanoidChar.Health / humanoidChar.MaxHealth
            end
            local color = getESPColor(plr)
            
            if espOffscreen and not onScreen then
                local direction = (hrp.Position - Camera.CFrame.Position).Unit
                local angle = math.atan2(direction.X, direction.Z)
                local arrowX = Camera.ViewportSize.X / 2 + math.sin(angle) * 100
                local arrowY = Camera.ViewportSize.Y / 2 + math.cos(angle) * 100
                local arrow = Drawing.new("Triangle")
                arrow.PointA = Vector2.new(arrowX, arrowY)
                arrow.PointB = Vector2.new(arrowX - 5, arrowY + 10)
                arrow.PointC = Vector2.new(arrowX + 5, arrowY + 10)
                arrow.Color = color
                arrow.Thickness = 1
                arrow.Filled = true
                arrow.Visible = true
                table.insert(drawingObjects, arrow)
            end
            
            if onScreen then
                if espBox2D then
                    local size = 100 / pos.Z
                    local box = Drawing.new("Square")
                    box.Position = Vector2.new(pos.X - size, pos.Y - size * 2)
                    box.Size = Vector2.new(size * 2, size * 3)
                    box.Thickness = 1
                    box.Color = color
                    box.Filled = false
                    box.Visible = true
                    table.insert(drawingObjects, box)
                end
                
                if espCornerBox then
                    local size = 80 / pos.Z
                    local positions = {
                        {Vector2.new(pos.X - size, pos.Y - size), Vector2.new(pos.X - size/2, pos.Y - size)},
                        {Vector2.new(pos.X + size/2, pos.Y - size), Vector2.new(pos.X + size, pos.Y - size)},
                        {Vector2.new(pos.X + size, pos.Y - size), Vector2.new(pos.X + size, pos.Y - size/2)},
                        {Vector2.new(pos.X + size, pos.Y + size/2), Vector2.new(pos.X + size, pos.Y + size)},
                        {Vector2.new(pos.X + size, pos.Y + size), Vector2.new(pos.X + size/2, pos.Y + size)},
                        {Vector2.new(pos.X - size/2, pos.Y + size), Vector2.new(pos.X - size, pos.Y + size)},
                        {Vector2.new(pos.X - size, pos.Y + size), Vector2.new(pos.X - size, pos.Y + size/2)},
                        {Vector2.new(pos.X - size, pos.Y - size/2), Vector2.new(pos.X - size, pos.Y - size)}
                    }
                    for _, posPair in pairs(positions) do
                        local line = Drawing.new("Line")
                        line.From = posPair[1]
                        line.To = posPair[2]
                        line.Thickness = 1.5
                        line.Color = color
                        line.Visible = true
                        table.insert(drawingObjects, line)
                    end
                end
                
                if espTracer then
                    local tracer = Drawing.new("Line")
                    tracer.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                    tracer.To = Vector2.new(pos.X, pos.Y)
                    tracer.Thickness = 1
                    tracer.Color = color
                    tracer.Visible = true
                    table.insert(drawingObjects, tracer)
                end
                
                if espName then
                    local nameText = Drawing.new("Text")
                    nameText.Text = plr.Name
                    nameText.Position = Vector2.new(pos.X, pos.Y - 70)
                    nameText.Size = 14
                    nameText.Color = Color3.fromRGB(255, 255, 255)
                    nameText.Center = true
                    nameText.Visible = true
                    table.insert(drawingObjects, nameText)
                end
                
                if espDistance then
                    local distance = (Camera.CFrame.Position - hrp.Position).Magnitude
                    local distText = Drawing.new("Text")
                    distText.Text = math.floor(distance) .. "m"
                    distText.Position = Vector2.new(pos.X, pos.Y - 55)
                    distText.Size = 11
                    distText.Color = color
                    distText.Center = true
                    distText.Visible = true
                    table.insert(drawingObjects, distText)
                end
                
                if espHealth then
                    local healthText = Drawing.new("Text")
                    healthText.Text = math.floor(healthPercent * 100) .. "%"
                    healthText.Position = Vector2.new(pos.X, pos.Y - 40)
                    healthText.Size = 11
                    healthText.Color = Color3.fromRGB(0, 255, 0)
                    healthText.Center = true
                    healthText.Visible = true
                    table.insert(drawingObjects, healthText)
                end
                
                if espHealthBar then
                    local bar = Drawing.new("Square")
                    bar.Position = Vector2.new(pos.X - 30, pos.Y - 65)
                    bar.Size = Vector2.new(60 * healthPercent, 5)
                    local red = 255 - (healthPercent * 255)
                    local green = healthPercent * 255
                    bar.Color = Color3.fromRGB(red, green, 0)
                    bar.Filled = true
                    bar.Visible = true
                    table.insert(drawingObjects, bar)
                    
                    local barBg = Drawing.new("Square")
                    barBg.Position = Vector2.new(pos.X - 30, pos.Y - 65)
                    barBg.Size = Vector2.new(60, 5)
                    barBg.Color = Color3.fromRGB(50, 50, 50)
                    barBg.Filled = true
                    barBg.Visible = true
                    table.insert(drawingObjects, barBg)
                end
                
                if espHeadDot then
                    local head = plr.Character:FindFirstChild("Head")
                    if head then
                        local headPos = Camera:WorldToScreenPoint(head.Position)
                        if headPos.Z > 0 then
                            local dot = Drawing.new("Circle")
                            dot.Position = Vector2.new(headPos.X, headPos.Y)
                            dot.Radius = 4
                            dot.Color = color
                            dot.Filled = true
                            dot.Thickness = 1
                            dot.Visible = true
                            table.insert(drawingObjects, dot)
                        end
                    end
                end
                
                if espChams and plr.Character then
                    for _, part in pairs(plr.Character:GetDescendants()) do
                        if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                            local chams = Instance.new("BoxHandleAdornment")
                            chams.Name = "Chams_" .. plr.Name
                            chams.Adornee = part
                            chams.Size = part.Size
                            chams.Color3 = color
                            chams.Transparency = 0.5
                            chams.ZIndex = 0
                            chams.AlwaysOnTop = true
                            chams.Parent = part
                            table.insert(espObjects, chams)
                        end
                    end
                end
                
                if espChamsOutline and plr.Character then
                    for _, part in pairs(plr.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            local outline = Instance.new("SelectionBox")
                            outline.Name = "Outline_" .. plr.Name
                            outline.Adornee = part
                            outline.Color3 = color
                            outline.LineThickness = 0.05
                            outline.Transparency = 0.7
                            outline.Parent = part
                            table.insert(espObjects, outline)
                        end
                    end
                end
                
                if espHighlight then
                    local highlight = Instance.new("Highlight")
                    highlight.Name = "Highlight_" .. plr.Name
                    highlight.FillColor = color
                    highlight.FillTransparency = 0.7
                    highlight.OutlineColor = color
                    highlight.OutlineTransparency = 0.3
                    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    highlight.Parent = plr.Character
                    table.insert(highlightObjects, highlight)
                end
            end
        end
    end
end

-- ========== AIMBOT FUNCTIONS ==========
local function createFOVCircle()
    if aimbotFOVCircle then
        aimbotFOVCircle:Remove()
    end
    if not aimbotFOV then
        return
    end
    aimbotFOVCircle = Drawing.new("Circle")
    aimbotFOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    aimbotFOVCircle.Radius = aimbotFOVSize
    aimbotFOVCircle.Thickness = 1
    aimbotFOVCircle.Color = Color3.fromRGB(255, 255, 255)
    aimbotFOVCircle.Filled = false
    aimbotFOVCircle.Visible = true
end

local function getClosestPlayer()
    local closest = nil
    local closestDist = aimbotFOVSize
    if not aimbotFOV then
        closestDist = math.huge
    end
    
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = plr.Character.HumanoidRootPart
            local pos, onScreen = Camera:WorldToScreenPoint(hrp.Position)
            if onScreen then
                local mousePos = Vector2.new(mouse.X, mouse.Y)
                local targetPos = Vector2.new(pos.X, pos.Y)
                local dist = (mousePos - targetPos).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    closest = plr
                end
            end
        end
    end
    return closest
end

local function updateAimbot()
    if not aimbotEnabled then
        return
    end
    local target = getClosestPlayer()
    if not target then
        return
    end
    local targetPos = target.Character.HumanoidRootPart.Position
    
    if aimbotStandard then
        if aimbotSmooth then
            local currentCF = Camera.CFrame
            local targetCF = CFrame.new(Camera.CFrame.Position, targetPos)
            local smoothValue = 1 / aimbotSmoothValue
            local smoothCF = currentCF:Lerp(targetCF, smoothValue)
            Camera.CFrame = smoothCF
        else
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
        end
    end
    
    if aimbotLock then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
    end
end

local function silentAim()
    if not silentAimEnabled then
        return
    end
    local target = getClosestPlayer()
    if not target then
        return
    end
    local targetPos = target.Character.HumanoidRootPart.Position
    local direction = (targetPos - Camera.CFrame.Position).Unit
    
    for _, v in pairs(ReplicatedStorage:GetChildren()) do
        if v:IsA("RemoteEvent") then
            local nameLower = string.lower(v.Name)
            if string.find(nameLower, "shoot") or string.find(nameLower, "fire") then
                pcall(function() v:FireServer(direction) end)
            end
        end
    end
end

-- ========== MOVEMENT HACKS ==========
local function toggleSpeed()
    speedEnabled = not speedEnabled
    if speedEnabled then
        humanoid.WalkSpeed = speedValue
        toast("Speed: " .. speedValue)
    else
        humanoid.WalkSpeed = 16
        toast("Speed disabled")
    end
end

local function setSpeed(val)
    speedValue = val
    if speedEnabled then
        humanoid.WalkSpeed = speedValue
    end
    toast("Speed set to: " .. speedValue)
end

local function toggleJump()
    jumpEnabled = not jumpEnabled
    if jumpEnabled then
        humanoid.JumpPower = jumpValue
        toast("High Jump: " .. jumpValue)
    else
        humanoid.JumpPower = 50
        toast("High Jump disabled")
    end
end

local function setJump(val)
    jumpValue = val
    if jumpEnabled then
        humanoid.JumpPower = jumpValue
    end
    toast("Jump set to: " .. jumpValue)
end

local function toggleInfinityJump()
    infinityJumpEnabled = not infinityJumpEnabled
    if infinityJumpEnabled then
        if infinityJumpConn then
            infinityJumpConn:Disconnect()
        end
        infinityJumpConn = UserInputService.JumpRequest:Connect(function()
            if infinityJumpEnabled then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)
        toast("Infinity Jump ON")
    else
        if infinityJumpConn then
            infinityJumpConn:Disconnect()
        end
        toast("Infinity Jump OFF")
    end
end

local function updateFly()
    if not flyEnabled then
        return
    end
    if not rootPart or not rootPart.Parent then
        return
    end
    
    local direction = Vector3.new()
    local wPressed = UserInputService:IsKeyDown(Enum.KeyCode.W)
    local sPressed = UserInputService:IsKeyDown(Enum.KeyCode.S)
    local aPressed = UserInputService:IsKeyDown(Enum.KeyCode.A)
    local dPressed = UserInputService:IsKeyDown(Enum.KeyCode.D)
    local spacePressed = UserInputService:IsKeyDown(Enum.KeyCode.Space)
    local ctrlPressed = UserInputService:IsKeyDown(Enum.KeyCode.LeftControl)
    
    local forward = 0
    if wPressed then forward = 1 end
    if sPressed then forward = -1 end
    
    local right = 0
    if dPressed then right = 1 end
    if aPressed then right = -1 end
    
    local up = 0
    if spacePressed then up = 1 end
    if ctrlPressed then up = -1 end
    
    direction = (Camera.CFrame.LookVector * forward) + (Camera.CFrame.RightVector * right)
    direction = direction + Vector3.new(0, up, 0)
    
    if direction.Magnitude > 0 then
        direction = direction.Unit * 85
    end
    if flyBodyVelocity then
        flyBodyVelocity.VectorVelocity = direction
    end
end

local function toggleFly()
    flyEnabled = not flyEnabled
    if flyEnabled then
        humanoid.PlatformStand = true
        flyBodyVelocity = Instance.new("BodyVelocity")
        flyBodyVelocity.MaxForce = Vector3.new(10000, 10000, 10000)
        flyBodyVelocity.Parent = rootPart
        RunService.RenderStepped:Connect(updateFly)
        toast("Fly ON")
    else
        humanoid.PlatformStand = false
        if flyBodyVelocity then
            flyBodyVelocity:Destroy()
        end
        toast("Fly OFF")
    end
end

local function toggleNoclip()
    noclipEnabled = not noclipEnabled
    if noclipEnabled then
        for _, conn in pairs(noclipConnections) do
            conn:Disconnect()
        end
        noclipConnections = {}
        
        local function noclipFunc()
            if noclipEnabled and character and character.Parent then
                for _, part in pairs(character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end
        
        noclipConnections[1] = RunService.Stepped:Connect(noclipFunc)
        noclipConnections[2] = character.DescendantAdded:Connect(noclipFunc)
        toast("Noclip ON")
    else
        for _, conn in pairs(noclipConnections) do
            conn:Disconnect()
        end
        noclipConnections = {}
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
        toast("Noclip OFF")
    end
end

-- ========== AUTO FUNCTIONS ==========
local function toggleAutoClick()
    autoClickEnabled = not autoClickEnabled
    if autoClickEnabled then
        autoClickConnection = RunService.RenderStepped:Connect(function()
            if autoClickEnabled then
                for _, v in pairs(ReplicatedStorage:GetChildren()) do
                    if v:IsA("RemoteEvent") then
                        local nameLower = string.lower(v.Name)
                        if string.find(nameLower, "click") then
                            pcall(function() v:FireServer() end)
                        end
                    end
                end
            end
        end)
        toast("Auto Click ON")
    else
        if autoClickConnection then
            autoClickConnection:Disconnect()
        end
        toast("Auto Click OFF")
    end
end

local function toggleAutoFarm()
    autoFarmEnabled = not autoFarmEnabled
    if autoFarmEnabled then
        spawn(function()
            while autoFarmEnabled do
                for _, v in pairs(ReplicatedStorage:GetChildren()) do
                    if v:IsA("RemoteEvent") then
                        local nameLower = string.lower(v.Name)
                        if string.find(nameLower, "farm") or string.find(nameLower, "claim") then
                            pcall(function() v:FireServer() end)
                        end
                    end
                end
                task.wait(0.1)
            end
        end)
        toast("Auto Farm ON")
    else
        toast("Auto Farm OFF")
    end
end

-- ========== CREATE UI ==========
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DragonPingX"
screenGui.ResetOnSpawn = false
local success = pcall(function()
    screenGui.Parent = game:GetService("CoreGui")
end)
if not success then
    pcall(function()
        screenGui.Parent = player.PlayerGui
    end)
end

-- Floating button
local toggleFrame = Instance.new("Frame")
toggleFrame.Size = UDim2.new(0, 55, 0, 55)
toggleFrame.Position = UDim2.new(0.85, 0, 0.07, 0)
toggleFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 150)
toggleFrame.BackgroundTransparency = 0.2
toggleFrame.BorderSizePixel = 2
toggleFrame.BorderColor3 = Color3.fromRGB(255, 255, 255)
toggleFrame.Visible = false
toggleFrame.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 28)
toggleCorner.Parent = toggleFrame

local toggleImage = Instance.new("ImageLabel")
toggleImage.Size = UDim2.new(0.8, 0, 0.8, 0)
toggleImage.Position = UDim2.new(0.1, 0, 0.1, 0)
toggleImage.BackgroundTransparency = 1
toggleImage.Image = ICON_URL
toggleImage.ScaleType = Enum.ScaleType.Fit
toggleImage.Parent = toggleFrame

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(1, 0, 1, 0)
toggleBtn.BackgroundTransparency = 1
toggleBtn.Text = ""
toggleBtn.Parent = toggleFrame

-- Login Menu
local loginFrame = Instance.new("Frame")
loginFrame.Size = UDim2.new(0, 420, 0, 380)
loginFrame.Position = UDim2.new(0.5, -210, 0.5, -190)
loginFrame.BackgroundColor3 = Color3.fromRGB(15, 20, 35)
loginFrame.BackgroundTransparency = 0.1
loginFrame.BorderSizePixel = 3
loginFrame.BorderColor3 = Color3.fromRGB(255, 0, 150)
loginFrame.Parent = screenGui

local loginTitle = Instance.new("TextLabel")
loginTitle.Size = UDim2.new(1, 0, 0, 50)
loginTitle.Position = UDim2.new(0, 0, 0, 0)
loginTitle.BackgroundColor3 = Color3.fromRGB(255, 0, 150)
loginTitle.BackgroundTransparency = 0.2
loginTitle.Text = "DRAGON PINGX LOGIN"
loginTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
loginTitle.TextSize = 18
loginTitle.Font = Enum.Font.GothamBold
loginTitle.Parent = loginFrame

local loginLabel = Instance.new("TextLabel")
loginLabel.Size = UDim2.new(0.9, 0, 0, 30)
loginLabel.Position = UDim2.new(0.05, 0, 0.2, 0)
loginLabel.BackgroundTransparency = 1
loginLabel.Text = "Enter License Key:"
loginLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
loginLabel.TextSize = 14
loginLabel.Font = Enum.Font.GothamBold
loginLabel.TextXAlignment = Enum.TextXAlignment.Center
loginLabel.Parent = loginFrame

local loginKeyBox = Instance.new("TextBox")
loginKeyBox.Size = UDim2.new(0.8, 0, 0, 45)
loginKeyBox.Position = UDim2.new(0.1, 0, 0.32, 0)
loginKeyBox.BackgroundColor3 = Color3.fromRGB(40, 45, 65)
loginKeyBox.PlaceholderText = "Paste your key here"
loginKeyBox.Text = ""
loginKeyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
loginKeyBox.TextSize = 13
loginKeyBox.Parent = loginFrame

local getKeyBtn = Instance.new("TextButton")
getKeyBtn.Size = UDim2.new(0.35, 0, 0, 40)
getKeyBtn.Position = UDim2.new(0.1, 0, 0.52, 0)
getKeyBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 200)
getKeyBtn.Text = "GET KEY"
getKeyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
getKeyBtn.TextSize = 14
getKeyBtn.Font = Enum.Font.GothamBold
getKeyBtn.Parent = loginFrame

local loginBtn = Instance.new("TextButton")
loginBtn.Size = UDim2.new(0.35, 0, 0, 40)
loginBtn.Position = UDim2.new(0.55, 0, 0.52, 0)
loginBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
loginBtn.Text = "LOGIN"
loginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
loginBtn.TextSize = 14
loginBtn.Font = Enum.Font.GothamBold
loginBtn.Parent = loginFrame

local loginStatus = Instance.new("TextLabel")
loginStatus.Size = UDim2.new(0.9, 0, 0, 30)
loginStatus.Position = UDim2.new(0.05, 0, 0.75, 0)
loginStatus.BackgroundTransparency = 1
loginStatus.Text = "Waiting for key..."
loginStatus.TextColor3 = Color3.fromRGB(255, 200, 100)
loginStatus.TextSize = 11
loginStatus.Parent = loginFrame

-- Main Menu
local main = Instance.new("Frame")
main.Size = UDim2.new(0, 500, 0, 580)
main.Position = menuPosition
main.BackgroundColor3 = Color3.fromRGB(15, 20, 35)
main.BackgroundTransparency = 0.1
main.BorderSizePixel = 2
main.BorderColor3 = Color3.fromRGB(255, 0, 150)
main.Visible = false
main.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = main

local glowFrame = Instance.new("Frame")
glowFrame.Size = UDim2.new(1, 10, 1, 10)
glowFrame.Position = UDim2.new(-0.01, 0, -0.01, 0)
glowFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 150)
glowFrame.BackgroundTransparency = 0.95
glowFrame.BorderSizePixel = 0
glowFrame.Parent = main

local glowCorner = Instance.new("UICorner")
glowCorner.CornerRadius = UDim.new(0, 18)
glowCorner.Parent = glowFrame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 45)
titleBar.BackgroundColor3 = Color3.fromRGB(255, 0, 150)
titleBar.BackgroundTransparency = 0.2
titleBar.Parent = main

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 12)
titleCorner.Parent = titleBar

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(1, -80, 1, 0)
titleText.Position = UDim2.new(0, 10, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "DRAGON PINGX ULTIMATE"
titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
titleText.TextSize = 16
titleText.Font = Enum.Font.GothamBold
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Parent = titleBar

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 35, 1, 0)
closeBtn.Position = UDim2.new(1, -35, 0, 0)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 100)
closeBtn.BackgroundTransparency = 0.3
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 18
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.Parent = titleBar

-- Scrolling Frame
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Size = UDim2.new(0.96, 0, 0.78, 0)
scrollFrame.Position = UDim2.new(0.02, 0, 0.1, 0)
scrollFrame.BackgroundTransparency = 1
scrollFrame.ScrollBarThickness = 5
scrollFrame.Parent = main

local content = Instance.new("Frame")
content.Size = UDim2.new(1, 0, 0, 0)
content.BackgroundTransparency = 1
content.Parent = scrollFrame

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 6)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Parent = content

local function addCategory(title)
    local cat = Instance.new("TextLabel")
    cat.Size = UDim2.new(1, -10, 0, 30)
    cat.BackgroundColor3 = Color3.fromRGB(255, 0, 150)
    cat.BackgroundTransparency = 0.3
    cat.Text = "📁 " .. title
    cat.TextColor3 = Color3.fromRGB(255, 255, 255)
    cat.TextSize = 13
    cat.Font = Enum.Font.GothamBold
    cat.TextXAlignment = Enum.TextXAlignment.Center
    cat.Parent = content
    return cat
end

local function addButton(text, callback, color)
    if color == nil then
        color = Color3.fromRGB(100, 50, 150)
    end
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 38)
    btn.BackgroundColor3 = color
    btn.BackgroundTransparency = 0.4
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = content
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local function addSlider(name, minVal, maxVal, defaultVal, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 50)
    frame.BackgroundColor3 = Color3.fromRGB(25, 30, 50)
    frame.BackgroundTransparency = 0.4
    frame.Parent = content
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 0, 25)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name .. ": " .. defaultVal
    label.TextColor3 = Color3.fromRGB(200, 200, 255)
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    
    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0.25, 0, 0, 30)
    box.Position = UDim2.new(0.5, 0, 0.1, 0)
    box.BackgroundColor3 = Color3.fromRGB(40, 45, 65)
    box.Text = tostring(defaultVal)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.TextSize = 11
    box.Parent = frame
    
    local apply = Instance.new("TextButton")
    apply.Size = UDim2.new(0.2, 0, 0, 30)
    apply.Position = UDim2.new(0.78, 0, 0.1, 0)
    apply.BackgroundColor3 = Color3.fromRGB(0, 150, 100)
    apply.Text = "SET"
    apply.TextColor3 = Color3.fromRGB(255, 255, 255)
    apply.TextSize = 11
    apply.Parent = frame
    
    apply.MouseButton1Click:Connect(function()
        local val = tonumber(box.Text)
        if val then
            if val < minVal then val = minVal end
            if val > maxVal then val = maxVal end
            box.Text = tostring(val)
            label.Text = name .. ": " .. val
            callback(val)
        end
    end)
end

local function addToggle(text, getter, setter)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 38)
    btn.BackgroundColor3 = Color3.fromRGB(100, 50, 150)
    btn.BackgroundTransparency = 0.4
    btn.Text = text .. ": OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = content
    
    local function update()
        if getter() then
            btn.Text = text .. ": ON"
        else
            btn.Text = text .. ": OFF"
        end
    end
    update()
    
    btn.MouseButton1Click:Connect(function()
        setter()
        update()
    end)
    return btn
end

-- Build Categories
addCategory("💀 SPECIAL FREEZE")
addButton("🔥 START FREEZE (KING MODE)", startFreeze, Color3.fromRGB(200, 50, 50))
addButton("✅ STOP FREEZE", stopFreeze, Color3.fromRGB(50, 150, 50))
addButton("🔄 TOGGLE FREEZE", toggleFreeze, Color3.fromRGB(150, 100, 200))

addCategory("🎯 AIMBOT")
addToggle("Aimbot Standard", function() return aimbotStandard end, function() aimbotStandard = not aimbotStandard; aimbotEnabled = aimbotStandard or aimbotLock or aimbotSmooth end)
addToggle("Aimbot Lock", function() return aimbotLock end, function() aimbotLock = not aimbotLock; aimbotEnabled = aimbotStandard or aimbotLock or aimbotSmooth end)
addToggle("Aimbot Smooth", function() return aimbotSmooth end, function() aimbotSmooth = not aimbotSmooth; aimbotEnabled = aimbotStandard or aimbotLock or aimbotSmooth end)
addToggle("Silent Aim", function() return silentAimEnabled end, function() silentAimEnabled = not silentAimEnabled end)
addToggle("Aimbot FOV", function() return aimbotFOV end, function() aimbotFOV = not aimbotFOV; createFOVCircle() end)
addSlider("FOV Size", 50, 300, 150, function(val) aimbotFOVSize = val; createFOVCircle() end)
addSlider("Smooth Value", 2, 20, 5, function(val) aimbotSmoothValue = val end)

addCategory("👁️ ESP")
addToggle("Enable ESP", function() return espEnabled end, function() espEnabled = not espEnabled; if not espEnabled then clearESP() end end)
addToggle("2D Box", function() return espBox2D end, function() espBox2D = not espBox2D end)
addToggle("Corner Box", function() return espCornerBox end, function() espCornerBox = not espCornerBox end)
addToggle("Tracer", function() return espTracer end, function() espTracer = not espTracer end)
addToggle("Name", function() return espName end, function() espName = not espName end)
addToggle("Distance", function() return espDistance end, function() espDistance = not espDistance end)
addToggle("Health Text", function() return espHealth end, function() espHealth = not espHealth end)
addToggle("Health Bar", function() return espHealthBar end, function() espHealthBar = not espHealthBar end)
addToggle("Head Dot", function() return espHeadDot end, function() espHeadDot = not espHeadDot end)
addToggle("Chams Fill", function() return espChams end, function() espChams = not espChams end)
addToggle("Chams Outline", function() return espChamsOutline end, function() espChamsOutline = not espChamsOutline end)
addToggle("Highlight XRay", function() return espHighlight end, function() espHighlight = not espHighlight end)
addToggle("Off-screen Arrow", function() return espOffscreen end, function() espOffscreen = not espOffscreen end)
addToggle("Team Check", function() return espTeamCheck end, function() espTeamCheck = not espTeamCheck end)
addToggle("Rainbow Color", function() return espRainbow end, function() espRainbow = not espRainbow end)

addCategory("🏃 MOVEMENT HACKS")
addToggle("Speed Hack", function() return speedEnabled end, toggleSpeed)
addSlider("Speed Value", 16, 250, 50, setSpeed)
addToggle("High Jump", function() return jumpEnabled end, toggleJump)
addSlider("Jump Value", 50, 500, 150, setJump)
addToggle("Infinity Jump", function() return infinityJumpEnabled end, toggleInfinityJump)
addToggle("Fly", function() return flyEnabled end, toggleFly)
addToggle("Noclip", function() return noclipEnabled end, toggleNoclip)

addCategory("🖱️ AUTO & FARM")
addToggle("Auto Click", function() return autoClickEnabled end, toggleAutoClick)
addToggle("Auto Farm", function() return autoFarmEnabled end, toggleAutoFarm)

addCategory("ℹ️ INFORMATION")
addButton("🆔 GET HWID", function()
    toast("HWID: " .. getHWID())
end)
addButton("🔗 COPY WEBSITE LINK", function()
    setclipboard(API_URL)
    toast("Link copied: " .. API_URL)
end)

local footer = Instance.new("TextLabel")
footer.Size = UDim2.new(1, 0, 0, 25)
footer.Position = UDim2.new(0, 0, 1, -25)
footer.BackgroundTransparency = 1
footer.Text = "🐉 ADMIN KEY: QANHNO1CRACKER | Press X to toggle menu"
footer.TextColor3 = Color3.fromRGB(200, 200, 100)
footer.TextSize = 9
footer.Parent = main

layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    content.Size = UDim2.new(1, 0, 0, layout.AbsoluteContentSize.Y + 20)
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 30)
end)

-- ========== ANIMATIONS ==========
local function animateFadeOut(frame, callback)
    local tween = TweenService:Create(frame, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundTransparency = 1})
    tween:Play()
    tween.Completed:Connect(function()
        frame.Visible = false
        if callback then
            callback()
        end
    end)
end

local function showMainMenu()
    animateFadeOut(loginFrame, function()
        main.Visible = true
        toggleFrame.Visible = true
        isLoggedIn = true
        toast("🐉 Welcome to DRAGON PINGX ULTIMATE!")
        notify("DRAGON PINGX ULTIMATE - All Features Loaded")
    end)
end

-- Get Key button
getKeyBtn.MouseButton1Click:Connect(function()
    local url = API_URL .. "/getkey"
    setclipboard(url)
    loginStatus.Text = "Link copied! Paste in browser"
    loginStatus.TextColor3 = Color3.fromRGB(255, 200, 100)
    task.wait(2)
    loginStatus.Text = "Waiting for key..."
    loginStatus.TextColor3 = Color3.fromRGB(255, 200, 100)
end)

-- Login button
loginBtn.MouseButton1Click:Connect(function()
    local key = loginKeyBox.Text
    if key == "" then
        loginStatus.Text = "Please enter a key!"
        loginStatus.TextColor3 = Color3.fromRGB(255, 100, 100)
        return
    end
    loginStatus.Text = "Verifying key..."
    loginStatus.TextColor3 = Color3.fromRGB(255, 200, 100)
    if verifyKey(key) then
        isVerified = true
        loginStatus.Text = "Login successful!"
        loginStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
        showMainMenu()
    else
        loginStatus.Text = "Invalid key!"
        loginStatus.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- Close main menu
closeBtn.MouseButton1Click:Connect(function()
    menuVisible = false
    main.Visible = false
    toggleFrame.Visible = false
    if freezeActive then
        stopFreeze()
    end
    toast("Menu closed. Click dragon icon or press X to open.")
end)

-- Toggle menu via floating button
toggleBtn.MouseButton1Click:Connect(function()
    if not isLoggedIn then
        return
    end
    menuVisible = not menuVisible
    main.Visible = menuVisible
    if menuVisible then
        toast("Menu opened")
    else
        toast("Menu closed")
    end
end)

-- Toggle menu via keyboard X
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed or not isLoggedIn then
        return
    end
    if input.KeyCode == Enum.KeyCode.X then
        menuVisible = not menuVisible
        main.Visible = menuVisible
        if menuVisible then
            toast("Menu opened")
        else
            toast("Menu closed")
        end
    end
end)

-- Drag main menu
local dragging = false
local dragStart, startPos
titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = main.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        menuPosition = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        main.Position = menuPosition
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- Drag floating button
local draggingBtn = false
local dragStartBtn, startPosBtn
toggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingBtn = true
        dragStartBtn = input.Position
        startPosBtn = toggleFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if draggingBtn and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStartBtn
        toggleFrame.Position = UDim2.new(startPosBtn.X.Scale, startPosBtn.X.Offset + delta.X, startPosBtn.Y.Scale, startPosBtn.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingBtn = false
    end
end)

-- Animate floating button pulse
spawn(function()
    while screenGui and screenGui.Parent do
        if isLoggedIn then
            for i = 1, 2 do
                local size = 55 + math.sin(tick() * 5) * 3
                local tween = TweenService:Create(toggleFrame, TweenInfo.new(0.2), {Size = UDim2.new(0, size, 0, size)})
                tween:Play()
                task.wait(0.2)
            end
            task.wait(1)
        else
            task.wait(0.5)
        end
    end
end)

-- Auto update ESP and Aimbot
RunService.RenderStepped:Connect(function()
    updateESP()
    updateAimbot()
    silentAim()
end)

toast("🐉 DRAGON PINGX ULTIMATE LOADED - Please login", 4)